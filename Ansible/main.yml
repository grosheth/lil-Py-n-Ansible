---
- name: Preparation du serveur
  hosts: all
  remote_user: root
  vars_files: vars.yml

  tasks:
  - name: Install or update pip
    community.general.easy_install:
      name: pip
      state: latest

