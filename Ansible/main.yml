---
- name: Preparation du serveur
  hosts: all
  remote_user: root
  vars_files: vars.yml

  tasks:
  - name: install python
    raw: 'cat < /home/alex/projets/Python-Ansible/Ansible/script.sh'
    args:
      executable: /bin/bash

