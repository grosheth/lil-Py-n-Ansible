---
- name: Preparation du serveur
  hosts: all
  remote_user: root
  vars_files: vars.yml

  tasks:
  - name: install python
    command: apt install python3

  - name: Install bottle python package
    pip:
      name: bottle
